<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="OmniClient.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:OmniClient.Pages"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Jobs">
        <!--<FlyoutItem.Icon>
            <FontImageSource FontFamily="FAS"
                             Color="{AppThemeBinding 
                                    Dark=White, 
                                    Light={StaticResource SystemGray5Dark}}"
                             Glyph="{x:Static fontAwesome:FontAwesomeIcons.MugHot}"/>
        </FlyoutItem.Icon>-->
        <ShellContent ContentTemplate="{DataTemplate local:JobsPage}"
            Route="Jobs" />
    </FlyoutItem>

    <FlyoutItem Title="Example 1">
        <!--<FlyoutItem.Icon>
            <FontImageSource FontFamily="FAS"
                             Color="{AppThemeBinding 
                                    Dark=White, 
                                    Light={StaticResource SystemGray5Dark}}"
                             Glyph="{x:Static fontAwesome:FontAwesomeIcons.MugHot}"/>
        </FlyoutItem.Icon>-->
        <ShellContent ContentTemplate="{DataTemplate local:ExamplePage}"
            Route="Example" />
    </FlyoutItem>
   

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:ExamplePage}"
        Route="Example" />-->

</Shell>
